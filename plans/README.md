# Implementation Plans

Generated by the improve skill on 2026-08-25, against commit `a05b7fa`.
**Reconciled 2026-08-27 against `origin/main` (`334831e`).** Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

Repo-wide rules every executor must follow:

- Run `composer qa-check` before claiming done (rector dry-run, pint, phpstan, pest).
- Read `.ai/docs/invariants.md` before changing anything in `src/Run/`, `src/Runner/` or `src/Mcp/`.
- Commit signing is enabled; never fall back to an unsigned commit.
- Commit messages: plain imperative sentence, no conventional-commit prefix.
- Public examples/specs stay synthetic and provenance-free (see `AGENTS.md`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make a numeric step id survive the batch runner and the staleness check | P1 | S | — | DONE (commit `467cbb3`) |
| 002 | Stop a web request from reading `$_SERVER['argv']` unconditionally | P1 | S | — | DONE (commit `32e58ca`) |
| 003 | Silence the log write so a failed write cannot discard a real verdict | P1 | S | — | DONE (commit `a722a0e`) |
| 004 | Add the `class_exists` boot guard the design record says already exists | P1 | S | — | DONE (commit `2343c8c`) |
| 005 | Write the receipt atomically so a concurrent reader cannot see a torn file | P2 | S | — | TODO |
| 006 | Test Laravel 12 at its ceiling, not only its floor | P2 | S | — | DONE (commit `5c9ce37`) |
| 008 | Extract the receipt-acceptance policy out of `VerifyCommand` | P2 | M | — | TODO |
| 007 | Design a machine-readable receipt read path (spike) | P2 | M | 008 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies. Soft ordering: 001–004 are user-facing crash/outage fixes and go first; 005–006 are hardening; 007 is a design spike that can run any time.
- Plan 003 has landed; its chmod-unwritable-directory test in `tests/Unit/LogWriterTest.php` carries the root-skip guard plan 005 should reuse verbatim.
- **007 now depends on 008.** Building `--json` over the un-extracted guards means duplicating them; 008 turns `--json` into a serializer over `VerifyResult`. Execute 008 first. The row order above reflects this — 008 runs before 007 despite the number.

## Reconcile log — 2026-08-27

**Update, same day:** the verify-policy extraction was promoted out of "audited, not planned" into **plan 008**, and 007 was re-sequenced to depend on it.

Verified against `origin/main` (`334831e`); tags `v0.10.2`, `v0.10.3`, `v0.10.4` shipped from this backlog.

**DONE plans re-verified on current HEAD** — all five still hold:

| Plan | Check | Result |
|---|---|---|
| 001 | `array-key` docblock + `(string) $stepId` cast + `settle($process, $step->id()` | present |
| 002 | `runningInConsole()` is the first statement in `isStarting()` | present |
| 003 | `@file_put_contents` in `LogWriter::write()` | present |
| 004 | `Mcp::class` in `PRODUCTION_CLASSES` + `firstMissingProduction()` in the provider | present |
| 006 | Laravel 12 `prefer-stable` matrix cell | present |

**TODO plans refreshed** (both findings re-confirmed present, excerpts and `Planned at` updated to `334831e`):

- **005** — `write()` is byte-identical; only line numbers moved (22-36 → 32-46) after a `LEGACY_PATH` constant was added above the constructor. Refresh was cosmetic. Now also names `LEGACY_PATH` as out of scope.
- **007** — substantive drift. `VerifyCommand` gained `nothingRecorded()` (`:127`), splitting the no-receipt case into "nothing recorded" and "a pre-0.10.0 receipt exists and is deliberately unread". The plan's category list and test cases now cover both; all line references re-pinned. `Receipt::toArray()` and `specs/` unchanged, so the RQ3/RQ4 analysis stands as written.

**Nothing retired.** Every lead below was re-checked against `origin/main` and still exists.

## Findings considered and rejected

(So nobody re-audits them.)

- `open_run`'s `readOnlyHint: true` while it mutates in-process state: technically inaccurate, practically true (executes nothing, writes no files). Not worth changing.
- Duplicate pipeline names collapsing silently: documented as undetectable in the loaded array (README); a source-level static check would add config-parsing responsibility the package deliberately avoids.
- Every row of README "What it deliberately does not do" and the `.ai/docs/design-history.md` deferred/rejected lists: settled tradeoffs, not findings.

## Audited, not planned (this run)

High-confidence findings the operator did not select; still valid leads:

- Tree fingerprint captured 3–4× per tool call (`src/Run/Run.php:175,345,503`, `src/Mcp/StepPayload.php:141`) — latency plus two `stale` readings that can disagree in one response.
- README timeout examples (`1800`s at `README.md:628,635`) exceed the documented 600000 ms client wall clock with no caveat.
- `GitTreeFingerprint` rename/quoted-path parsing untested (`src/Runner/GitTreeFingerprint.php:88-104`) — a regression there fails open. Should land before anyone touches that parser.
- `laravel/mcp ^0.9.4` cannot reach the published `v1.0.0-beta.1`; migration spike needed before consumers move (plan 004's symbol list is the migration checklist).
- Log directory created at default `0777 & ~umask`; unredacted step output persisted indefinitely (`src/Runner/LogWriter.php:16,28`).
- "Resolve pipeline, refuse if no run" block triplicated across `NextStep`/`Status`/`ReportStep` handlers.
- Receipt-write swallow untested (plan 005 step 2 case 3 now covers it — no separate work needed); `Run::record()` crashes on an incomplete third-party batch-runner map; `symfony/process ^7.0` floor uninstallable by CI (real floor ^7.2); step output unbounded in memory; no coverage measurement in CI.
- Direction options not selected: hook-recipe generator, `close_run` tool + skill-step deadline, durable run state + lock, declared step inputs (per-step fingerprints).
